{
  "name": "@fountain/extension",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc && node build.js",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.251",
    "typescript": "^5.3.3"
  }
}

